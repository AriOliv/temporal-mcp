# Temporal SDK
temporalio>=1.5.0

# Model Context Protocol
mcp>=2.0.0

# Native streamable-HTTP transport (ASGI server + framework)
uvicorn>=0.30
starlette>=0.40

# Security floors for transitive runtime dependencies
idna>=3.15
python-multipart>=0.0.31

# Additional dependencies
asyncio-atexit>=1.0.1
